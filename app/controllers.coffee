'use strict'

angular.module('app.controllers', [
  'modules.utils'
])

angular.module('app.controllers')

.controller('PublicController', ($scope) ->

)

.controller('MainController', ($http, $scope, $state, Utils) ->

)
